export class UserWord {
	type: string;  //one of sound, phonetic, sight word 
	correctTimes: number; //how many times the user has correctly spelt the word
	spell: string;  // how the word is spelled
	
}